# Babylon Web Environment Variables

# Supabase Configuration
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Stripe Configuration (Get these from Stripe Dashboard: https://dashboard.stripe.com/apikeys)
# Use test keys for development (starts with pk_test_ and sk_test_)
# Use live keys for production (starts with pk_live_ and sk_live_)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Note: The Stripe secret key (sk_test_...) and webhook secret (whsec_...)
# should be set in Supabase Edge Function secrets, NOT here in the frontend .env
# Use: supabase secrets set STRIPE_SECRET_KEY=sk_test_...
# Use: supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
